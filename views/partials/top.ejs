<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= typeof title !== 'undefined' ? title : 'Local Orçamentos' %></title>
    <!-- CSS Original - Red/Black Theme -->
    <style>
      body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: #000; color: #fff; }
      .container { max-width: 1200px; margin: 0 auto; background: #1a1a1a; min-height: 100vh; }
      header { background: #c41e3a; color: white; padding: 20px; text-align: center; }
      header h1 { margin: 0; font-size: 2.5em; font-weight: bold; text-shadow: 2px 2px 4px rgba(0,0,0,0.5); }
      nav { margin: 15px 0 0 0; }
      nav a { color: #fff; text-decoration: none; margin: 0 20px; font-size: 18px; padding: 10px 20px; background: rgba(0,0,0,0.3); border-radius: 5px; }
      nav a:hover { background: rgba(0,0,0,0.6); }
      main { padding: 30px; }
      .card { background: #2a2a2a; padding: 25px; margin: 20px 0; border-radius: 10px; border: 2px solid #c41e3a; }
      .card h2 { color: #c41e3a; margin-top: 0; font-size: 1.8em; border-bottom: 2px solid #c41e3a; padding-bottom: 10px; }
      .btn { background: #c41e3a; color: white; padding: 12px 25px; border: none; border-radius: 5px; text-decoration: none; display: inline-block; margin: 5px 10px 5px 0; font-size: 16px; cursor: pointer; }
      .btn:hover { background: #a01729; }
      .btn.secondary { background: #666; }
      .btn.secondary:hover { background: #555; }
      table { width: 100%; border-collapse: collapse; margin: 20px 0; background: #2a2a2a; }
      th, td { border: 1px solid #c41e3a; padding: 12px; text-align: left; }
      th { background: #c41e3a; color: white; font-weight: bold; }
      td { background: #1a1a1a; }
      .field { margin-bottom: 20px; }
      .field label { display: block; margin-bottom: 8px; color: #c41e3a; font-weight: bold; font-size: 16px; }
      .field input, .field textarea, .field select { width: 100%; padding: 12px; border: 2px solid #c41e3a; border-radius: 5px; background: #1a1a1a; color: #fff; font-size: 16px; }
      .field input:focus, .field textarea:focus, .field select:focus { outline: none; border-color: #fff; background: #333; }
      .modalidade-container { border: 3px solid #c41e3a; padding: 25px; margin: 25px 0; border-radius: 10px; background: #2a2a2a; }
      .modalidade-container h2 { color: #c41e3a; font-size: 1.5em; margin-bottom: 20px; text-transform: uppercase; }
      .equipment-section { background: #1a1a1a; border: 2px solid #666; padding: 20px; margin: 15px 0; border-radius: 8px; }
      .equipment-section h4 { color: #c41e3a; margin-bottom: 15px; font-size: 1.2em; }
      .item_row { display: flex; align-items: center; margin-bottom: 10px; padding: 10px; background: #2a2a2a; border-radius: 5px; }
      .item_row input, .item_row select { margin-right: 10px; padding: 8px; border: 1px solid #c41e3a; background: #1a1a1a; color: #fff; border-radius: 3px; }
      .checkboxes { margin: 15px 0; padding: 15px; background: #1a1a1a; border-radius: 8px; border: 1px solid #666; }
      .checkboxes label { color: #fff; margin-right: 20px; display: inline-block; margin-bottom: 10px; cursor: pointer; }
      .checkboxes input[type="checkbox"] { margin-right: 8px; }
    </style>

    <!-- Bibliotecas para PDF -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="/js/pdf-generator.js"></script>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Local Orçamentos</h1>
        <nav>
          <a href="/">Início</a>
          <a href="/quotes/new">Nova Cotação</a>
        </nav>
      </header>
      <main>
