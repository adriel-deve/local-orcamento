<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= typeof title !== 'undefined' ? title : 'Local Orçamentos' %></title>
    <!-- CSS Moderno - Design Profissional Branco e Vermelho -->
    <style>
      :root {
        --primary-red: #dc2626;
        --primary-red-light: #ef4444;
        --primary-red-dark: #b91c1c;
        --white: #ffffff;
        --gray-50: #f9fafb;
        --gray-100: #f3f4f6;
        --gray-200: #e5e7eb;
        --gray-300: #d1d5db;
        --gray-400: #9ca3af;
        --gray-500: #6b7280;
        --gray-600: #4b5563;
        --gray-700: #374151;
        --gray-800: #1f2937;
        --gray-900: #111827;
        --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
        --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
        --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
        --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
      }

      * {
        box-sizing: border-box;
      }

      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        margin: 0;
        padding: 0;
        background: var(--gray-50);
        color: var(--gray-800);
        line-height: 1.6;
      }

      .container {
        max-width: 1400px;
        margin: 0 auto;
        background: var(--white);
        min-height: 100vh;
        box-shadow: var(--shadow-xl);
      }

      header {
        background: linear-gradient(135deg, var(--primary-red) 0%, var(--primary-red-dark) 100%);
        color: var(--white);
        padding: 32px 40px;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="white" opacity="0.05"/><circle cx="75" cy="75" r="1" fill="white" opacity="0.03"/><circle cx="50" cy="10" r="0.5" fill="white" opacity="0.04"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
        pointer-events: none;
      }

      header h1 {
        margin: 0;
        font-size: 2.8rem;
        font-weight: 700;
        letter-spacing: -0.025em;
        position: relative;
        z-index: 1;
      }

      nav {
        margin-top: 24px;
        position: relative;
        z-index: 1;
      }

      nav a {
        color: var(--white);
        text-decoration: none;
        margin: 0 12px;
        font-size: 1.1rem;
        padding: 12px 24px;
        background: rgba(255,255,255,0.15);
        border-radius: 50px;
        font-weight: 500;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255,255,255,0.2);
      }

      nav a:hover {
        background: rgba(255,255,255,0.25);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.2);
      }

      main {
        padding: 40px;
        max-width: none;
      }

      .card {
        background: var(--white);
        padding: 40px;
        margin: 24px 0;
        border-radius: 20px;
        border: 1px solid var(--gray-200);
        box-shadow: var(--shadow-lg);
        transition: all 0.3s ease;
      }

      .card:hover {
        box-shadow: var(--shadow-xl);
      }

      .card h2 {
        color: var(--primary-red);
        margin-top: 0;
        font-size: 2rem;
        font-weight: 700;
        border-bottom: 3px solid var(--primary-red);
        padding-bottom: 16px;
        margin-bottom: 32px;
        letter-spacing: -0.025em;
      }

      .card h3 {
        color: var(--gray-700);
        font-size: 1.25rem;
        font-weight: 600;
        margin-top: 32px;
        margin-bottom: 16px;
      }

      .card h4 {
        color: var(--gray-600);
        font-size: 1.125rem;
        font-weight: 600;
        margin-top: 24px;
        margin-bottom: 12px;
      }

      .btn {
        background: var(--primary-red);
        color: var(--white);
        padding: 14px 28px;
        border: none;
        border-radius: 12px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        margin: 8px 12px 8px 0;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: var(--shadow-sm);
      }

      .btn:hover {
        background: var(--primary-red-dark);
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
      }

      .btn:active {
        transform: translateY(0);
      }

      .btn.secondary {
        background: var(--gray-600);
        color: var(--white);
      }

      .btn.secondary:hover {
        background: var(--gray-700);
      }

      .btn.success {
        background: #059669;
      }

      .btn.success:hover {
        background: #047857;
      }

      .btn.primary {
        background: var(--primary-red);
      }

      .btn.danger {
        background: #dc2626;
        font-size: 0.875rem;
        padding: 8px 16px;
      }

      .btn.danger:hover {
        background: #b91c1c;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin: 24px 0;
        background: var(--white);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: var(--shadow-md);
      }

      th, td {
        border: 1px solid var(--gray-200);
        padding: 16px;
        text-align: left;
      }

      th {
        background: var(--primary-red);
        color: var(--white);
        font-weight: 600;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      td {
        background: var(--white);
        color: var(--gray-700);
      }

      tr:nth-child(even) td {
        background: var(--gray-50);
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 24px;
        margin: 24px 0;
      }

      .field {
        margin-bottom: 24px;
        position: relative;
      }

      .field label {
        display: block;
        margin-bottom: 8px;
        color: var(--gray-700);
        font-weight: 600;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      .field input, .field textarea, .field select {
        width: 100%;
        padding: 16px 20px;
        border: 2px solid var(--gray-200);
        border-radius: 12px;
        background: var(--white);
        color: var(--gray-800);
        font-size: 1rem;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: var(--shadow-sm);
        font-family: inherit;
      }

      .field input:focus, .field textarea:focus, .field select:focus {
        outline: none;
        border-color: var(--primary-red);
        background: var(--white);
        box-shadow: 0 0 0 3px rgba(220,38,38,0.1), var(--shadow-md);
        transform: translateY(-2px);
      }

      .field input:hover:not(:focus), .field textarea:hover:not(:focus), .field select:hover:not(:focus) {
        border-color: var(--gray-300);
      }

      .field input::placeholder, .field textarea::placeholder {
        color: var(--gray-400);
        font-style: normal;
      }

      .field small {
        color: var(--gray-500);
        font-size: 0.75rem;
        margin-top: 4px;
        display: block;
      }

      .modalidade-container {
        border: 3px solid var(--primary-red);
        padding: 32px;
        margin: 32px 0;
        border-radius: 16px;
        background: linear-gradient(135deg, rgba(220,38,38,0.02) 0%, rgba(220,38,38,0.05) 100%);
        position: relative;
      }

      .modalidade-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, var(--primary-red) 0%, var(--primary-red-light) 100%);
        border-radius: 16px 16px 0 0;
      }

      .modalidade-container h2 {
        color: var(--primary-red);
        font-size: 1.5rem;
        margin-bottom: 24px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.025em;
      }

      .equipment-section {
        background: var(--white);
        border: 2px solid var(--gray-200);
        padding: 24px;
        margin: 20px 0;
        border-radius: 12px;
        box-shadow: var(--shadow-sm);
        transition: all 0.3s ease;
      }

      .equipment-section:hover {
        border-color: var(--gray-300);
        box-shadow: var(--shadow-md);
      }

      .equipment-section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        padding: 16px;
        background: var(--gray-50);
        border-radius: 8px;
      }

      .equipment-section h4 {
        color: var(--primary-red);
        margin: 0;
        font-size: 1.125rem;
        font-weight: 600;
      }

      .item_row {
        display: grid;
        grid-template-columns: 2fr 80px 80px 120px 100px auto;
        gap: 16px;
        align-items: center;
        margin-bottom: 16px;
        padding: 20px;
        background: var(--white);
        border-radius: 12px;
        border: 2px solid var(--gray-100);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: var(--shadow-sm);
      }

      .item_row:hover {
        border-color: var(--primary-red);
        box-shadow: 0 4px 12px rgba(220,38,38,0.15);
        transform: translateY(-2px);
      }

      .item_row input, .item_row select {
        padding: 12px 16px;
        border: 2px solid var(--gray-200);
        background: var(--white);
        color: var(--gray-800);
        border-radius: 8px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        font-size: 0.875rem;
        font-family: inherit;
      }

      .item_row input:focus, .item_row select:focus {
        outline: none;
        border-color: var(--primary-red);
        box-shadow: 0 0 0 2px rgba(220,38,38,0.2);
      }

      .item_row input:hover:not(:focus), .item_row select:hover:not(:focus) {
        border-color: var(--gray-300);
      }

      .checkboxes {
        margin: 20px 0;
        padding: 20px;
        background: var(--gray-50);
        border-radius: 12px;
        border: 2px solid var(--gray-200);
      }

      .checkboxes label {
        color: var(--gray-700);
        margin-right: 24px;
        display: inline-flex;
        align-items: center;
        margin-bottom: 12px;
        cursor: pointer;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      .checkboxes label:hover {
        color: var(--primary-red);
      }

      .checkboxes input[type="checkbox"] {
        margin-right: 8px;
        width: 18px;
        height: 18px;
        accent-color: var(--primary-red);
      }

      .payment-conditions-container {
        border: 3px solid #059669;
        padding: 32px;
        margin: 32px 0;
        border-radius: 16px;
        background: linear-gradient(135deg, rgba(5,150,105,0.02) 0%, rgba(5,150,105,0.05) 100%);
        position: relative;
      }

      .payment-conditions-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #059669 0%, #10b981 100%);
        border-radius: 16px 16px 0 0;
      }

      .actions {
        display: flex;
        gap: 16px;
        margin-top: 40px;
        padding-top: 32px;
        border-top: 2px solid var(--gray-100);
        flex-wrap: wrap;
      }

      hr {
        border: none;
        height: 2px;
        background: linear-gradient(90deg, var(--primary-red) 0%, transparent 100%);
        margin: 32px 0;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        main {
          padding: 20px;
        }

        .card {
          padding: 24px;
          margin: 16px 0;
        }

        header {
          padding: 24px 20px;
        }

        header h1 {
          font-size: 2rem;
        }

        nav a {
          margin: 0 8px;
          padding: 10px 16px;
          font-size: 1rem;
        }

        .grid {
          grid-template-columns: 1fr;
          gap: 16px;
        }

        .item_row {
          grid-template-columns: 1fr;
          gap: 12px;
        }

        .actions {
          flex-direction: column;
        }

        .btn {
          width: 100%;
          justify-content: center;
        }
      }
    </style>

    <!-- Bibliotecas para PDF -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="/js/pdf-generator.js"></script>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Local Orçamentos</h1>
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <div style="position: absolute; top: 20px; right: 40px; color: white; text-align: right; z-index: 10;">
            <div style="font-size: 0.875rem; opacity: 0.9;">Bem-vindo,</div>
            <div style="font-weight: 600; font-size: 1rem;"><%= currentUser.fullName || currentUser.username %></div>
            <% if (currentUser.role === 'admin') { %>
              <div style="font-size: 0.75rem; background: rgba(255,255,255,0.2); padding: 2px 8px; border-radius: 4px; display: inline-block; margin-top: 4px;">ADMIN</div>
            <% } %>
          </div>
        <% } %>
        <nav>
          <a href="/">Início</a>
          <a href="/quotes/new">Nova Cotação</a>
          <a href="/quotes/drafts">Rascunhos</a>
          <a href="/quotes/completed">Cotações Concluídas</a>
          <% if (typeof currentUser !== 'undefined' && currentUser && currentUser.role === 'admin') { %>
            <a href="/users">👥 Gerenciar Usuários</a>
          <% } %>
          <% if (typeof currentUser !== 'undefined' && currentUser) { %>
            <a href="/logout">Sair</a>
          <% } %>
        </nav>
      </header>
      <main>
