<%- include('partials/top') %>
<section class="card error">
  <h2>Não encontrado</h2>
  <p><%= (typeof message !== 'undefined' && message) ? message : 'Recurso não encontrado.' %></p>
  <div class="actions">
    <a class="btn" href="/">Voltar</a>
  </div>
</section>
<%- include('partials/bottom') %>
